# PythonbatchNovDec2024
a repository with all the class materials to become a python developer

## git commands

To clone a repository to local
git clone   (here we can use https, ssh)
 this is not needed in codespace only needed in local
   git clone https://github.com/SaiDhulipalla8096/PythonbatchNovDec2024.git

To stage the changes:

 to create a branch 

 To list the branches we can do 
   git branch

   To create a new branch

   git checkpout -b branch name

To see the latest local changes

  use git status 

  To exactly see what changes we did on a single file

  git diff


  To stage the changes from local to repository

  git add README.md

  to commit the changes

  git commit -m "commit message"

  to push the changes

  git push origin branch name
  